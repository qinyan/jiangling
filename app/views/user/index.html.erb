<style type="text/css">
  .field_with_errors {
    display: inline;
  }
  
  .field_with_errors .message {
    color: #D00;
    padding-left: 5px;
    font-size: 12px;
  }
</style>
<%= form_for @user, :validations => true do |b| %>
  <%= b.label :username %></br>
  <%= b.text_field :username%></br>
  <%= submit_tag 'Create' %>
<% end %>
